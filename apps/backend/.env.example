# Database
DATABASE_URL="postgresql://username:password@localhost:5432/meeting_room_booking?schema=public"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=5000
NODE_ENV="development"

# CORS
FRONTEND_URL="http://localhost:3000"

# Email Configuration (Nodemailer)
SMTP_HOST=gtim-co-id0c.mail.protection.outlook.com
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME="Visitor Apps Notification"
SMTP_FROM_EMAIL=notification@g-tim.co.id
